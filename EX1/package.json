{
  "name": "ex1",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npx prisma generate && npx prisma migrate dev && node prisma/seed.js",
    "start": "node src/server.js",
    "dev": "node prisma/seed.js && nodemon src/server.js",
    "clean": "rmdir /s /q node_modules && del /f /q package-lock.json && npm cache clean --force && npm install"
  },
  "author": "Kyte Hoang",
  "license": "ISC",
  "description": "A simple Express server project",
  "dependencies": {
    "@prisma/client": "^6.17.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "morgan": "^1.10.1",
    "mysql2": "^3.15.1"
  },
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/ex1.git"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.17.0"
  }
}